import { Strategy } from "../strategy";
export interface MockStrategyVerifyCallback<User> {
    (): Promise<User>;
}
export declare class MockStrategy<User> extends Strategy<User, Record<string, never>> {
    name: string;
    authenticate(): Promise<User>;
}
